{
  "name": "sheild-ai",
  "private": true,
  "scripts": {
    "build": "npm --prefix apps/web run build",
    "start": "npm --prefix apps/web run start"
  },
  "dependencies": {
    "@remix-run/dev": "^2.17.5"
  },
  "engines": {
    "node": "22.x"
  }
}
