{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "bun install && cd apps/web && bun install",
  "buildCommand": "cd apps/web && npm run build && cd ../.. && node -e \"require('fs').cpSync('apps/web/build','build',{recursive:true,force:true})\"",
  "framework": "react-router"
}
